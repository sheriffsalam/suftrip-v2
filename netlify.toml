[build]
  publish = "web"

[[redirects]]
  from = "/api/*"
  to = "https://SUFTRIP_API_HOST/api/:splat"
  status = 200
  force = true

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer"
